Make the smallest clear change that satisfies the goal.
Keep public behavior compatible unless the goal explicitly requests a change.
Include tests for new edge cases. Never delete or weaken existing assertions.
Avoid new dependencies unless the goal requires them.
Record specific failure causes, not vague advice, in lessons.
